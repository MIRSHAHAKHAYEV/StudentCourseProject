spring.application.version=0.01
app.name=CourseProject
app.maximum-student=${APP_MAXIMUM_STUDENT:50}
app.course-default-duration=${APP_COURSE_DEFAULT_DURATION:12}
app.enabled=${APP_ENABLED:true}
app.allowed-countries=${APP_ALLOWED_COUNTRIES:AZ,TR,US}
spring.jpa.properties.hibernate.default_schema=studentcourse
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=${DB_PASSWORD:slmokbb123}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
server.port=8080
app.base-url=http://localhost:${server.port}

